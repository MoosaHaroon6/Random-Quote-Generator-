# clone 
 git clone <repoUrl>

# install shadcn 
 // npx shadcn@latest init -d

# packages (shadcn)
   => install button
   => install toast
   => install card
   => install label
   => install toaster

# node_modules
 // npm i

# Api
 // http://api.quotable.io/random

# run 
 // npm run dev 
  
